# GoSearch

*(Work in Progress)*

A set of tools written in Go for searching through files, applications, and other user defined collections 
and then executing commands on the results.

This is originally written as a backend for a MacOS application, but could also be integrated into a cli tool.
